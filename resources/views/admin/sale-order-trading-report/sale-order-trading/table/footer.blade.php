{{-- <tr>
    <td colspan="9" class="text-center">Total</td>
    <td class="text-end">{{ $formatNumber ? formatNumber($item->sum('total')) : $item->sum('total') }}</td>
    <td></td>
    <td class="text-end">{{ $formatNumber ? formatNumber($item->sum('total_final')) : $item->sum('total_final') }}</td>
    <td></td>
</tr> --}}
